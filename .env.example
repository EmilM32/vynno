# Same-origin API prefix (Kit proxies to vynno-api). Use an absolute
# https://…/v1 only when the API shares the session cookie with the SPA.
PUBLIC_API_BASE=/v1

# Upstream API origin for the /v1 proxy (server-only).
# API_ORIGIN=http://localhost:8080

# Optional. Playwright e2e registers throwaway users by default.
# Set these only if you want a spec to sign in as the bootstrap account.
# E2E_USERNAME=alexdev
# E2E_PASSWORD=local-dev-password
