# Public API origin + version prefix (no trailing slash).
# Default /mock/v1 serves GET + writes (header-scoped mock workspace).
# Point at the live origin (including /v1) when the backend exists.
PUBLIC_API_BASE=/mock/v1
