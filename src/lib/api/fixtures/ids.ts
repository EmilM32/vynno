/** Stable mock ids — referenced across fixtures and tests. */
export const PROJECT_IDS = {
	auth: 'proj-auth',
	alpha: 'proj-alpha',
	ui: 'proj-ui',
	api: 'proj-api',
	internal: 'proj-internal'
} as const;
