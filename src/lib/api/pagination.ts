/** First Logs / boot page. Within the 10–15 range the API was sized for. */
export const SESSION_PAGE_SIZE = 15;

/** Matches the API default when `limit` is omitted. */
export const SESSION_LIST_DEFAULT_LIMIT = 20;
